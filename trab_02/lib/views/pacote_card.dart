import 'package:flutter/material.dart';

class PacoteCard extends StatelessWidget {
  const PacoteCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Card Pacote'),
      ),
    );
  }
}